#include "main.h"
#include <stdlib.h>

/**
  *alloc_grid - creates 2d array dinamically
  *@width: column of the array
  *@lenght: rows of the array
  *
  * Return: pointer to the 2-D array, NULL (on failure or
  * if width or height is 0)
  */

int **alloc_grid(int width, int height)
{

	int i, j, **grid;

	if (i || j <= 0)
		return (NULL);

}
