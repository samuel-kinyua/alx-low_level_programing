#include "main.h"

/**
 * _isupper - checks if int c is uppercase
 *
 * @c: integer to be checked
 *
 * Return: 1 when uppecase
 * 0 when not
 */

int _isupper(int c)
{

	if ((c >= 'A') && (c <= 'Z')
	{
		return (1);
	}
	return (0);

}
