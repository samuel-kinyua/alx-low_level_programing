#include <stdio.h>
/**
 * main - this is the entry point
 *Return: always retur 0 to show it was successful
 */

int main(void)
{
	char alpha = 'a';

	while (alpha <= 'z')

	{

		putchar(alpha);

		alpha++;

	}

	putchar('\n');



	return (0);
}
