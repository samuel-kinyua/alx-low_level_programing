#include <stdio.h>
/**
 * main - this is the entry point
 * Return: Always 0 to show that it excecuted successfully
 */

int main(void)
{
	int numbers = 0;

	while (numbers == 0)
	{
		printf("%d", numbers);
			numbers++;
	}
	return (0);
}
