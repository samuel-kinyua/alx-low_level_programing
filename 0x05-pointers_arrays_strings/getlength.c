#include "main.h"

/**
  *_strlen - gets the lenght of a string
  *@s: string whose length is being checked
  *
  *Return: length of string
  */

int _strlen(char *s)
{
	int length = 0;

	while (*s != '\0')
	{
		i++;
		*s++;

	}
	return (length);
}
